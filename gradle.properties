# Project properties
version=1.0.0-SNAPSHOT


# Gradle daemon settings
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# Spring profile (can be overridden via command line)
spring.profiles.active=local
